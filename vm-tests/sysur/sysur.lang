process soft {;}

process Ci=75 Ti=500
{
    input a;
    input b;
    input c;
    input d;
    input e;
    input f;

    x = (c*e - b*f) / (a*e - b*d);
    y = (a*f - c*d) / (a*e - b*d);

    output x;
    output y;
}

process Ci=323 Ti=500
{
    a = 0;
    while(a < 20)
    {
        a = a+1;
        if(a == 14)
        {
            return;
        }
    }
}

