process soft {;}

process Ci=500 Ti=1000
{
    input a;
    input b;
    input c;
    input d;
    input e;
    input f;

    x = (c*e - b*f) / (a*e - b*d);
    y = (a*f - c*d) / (a*e - b*d);

    output x;
    output y;
}

process Ci=100 Ti=700
{
    a = 0;
    while(a < 20)
    {
        a = a+1;
        if(a == 14)
        {
            return;
        }
    }
}

